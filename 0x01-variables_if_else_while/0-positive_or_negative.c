#include <stdio.h>
/**
 * main - checks if the variable n is positive or negative
 *
 * Return: 0 after successful exectuion
 */
int main(void)
{
if(n>0)
	printf("is positive");
else if(n==0)
	printf("is zero");
else
	printf("is negative");
return (0);
}
