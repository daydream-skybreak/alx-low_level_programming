#include <stdio.h>
#include "main.h"
/**
 * main - main function
 * @argc: number of command lines
 * @argv: array of string of command lines
 * Return: 1
 */
int main(int argc, char* argv[])
{
	printf("%s\n", argv[0]);
	return (1);
}
