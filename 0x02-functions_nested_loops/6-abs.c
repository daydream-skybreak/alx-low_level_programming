#include "main.h"
/**
 * _abs - computes absolute value
 * @int: integer parameter
 *
 * Return: returns hte absolute value of the number
 */
int _abs(int)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);
}
